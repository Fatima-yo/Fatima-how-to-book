
====================================
vibecoding in server with Claude
====================================

1. open terminal
2. cd 'Downloads/FATIMA - WORK/TACC/'

3.
//ssh -i id_rsa cameron@34.61.176.24 password: concha123.
ssh -i id_rsa cameron@34.61.84.128 password: concha123.

4. cd /var/www/tac
5. sudo su password: nomeimporta

6. claude
7. you can edit on the fly

------------------------------------

~/.claude/projects/-var-www/memory/MEMORY.md


------------------------------------

cd /var/www
sudo su
claude
bash: claude: command not found

which claude
(shows nothing)

type claude
bash: type: claude: not found

sudo find / -type f -name claude 2>/dev/null
(shows nothing)

echo $PATH
/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin

whoami
root

pipx list
bash: pipx: command not found

curl -fsSL https://claude.ai/install.sh | bash
Claude Code successfully installed!
  Version: 2.1.50
  Location: ~/.local/bin/claude
  Next: Run claude --help to get started

⚠ Setup notes:
  • Native installation exists but ~/.local/bin is not in your PATH. Run:
  echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc && source ~/.bashrc

(so it is installed at: /root/.local/bin/claude)

claude


====================================
*** end of document ***
====================================

